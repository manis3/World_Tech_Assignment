export enum ErrorCode {
  USER_NOT_FOUND = 404,
  USER_ALREADY_EXIST = 409,
  INVALID_REQUEST = 400,
  INVALID_DATA = 422,
  INTERNAL_SERVER_ERROR = 500,
  UNAUTHORIZED = 401,
  UNACCESSIBLE = 403,
}
